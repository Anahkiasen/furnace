<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<a class="navbar-brand">Furnace</a>
		</div>

		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<li {% if request.getPathInfo() == '/' %}class="active"{% endif %}>
					<a href="{{ URL.action('TracksController@index') }}">Tracks</a>
				</li>
				<li {% if request.getPathInfo() == '/trackers' %}class="active"{% endif %}>
					<a href="{{ URL.action('TrackersController@index') }}">Blacksmiths</a>
				</li>
			</ul>

			<ul class="nav navbar-nav navbar-right">
				{% if Auth.guest() %}
					<li><a href="#">Login</a></li>
					<li><a href="{{ URL.action('UsersController@signup') }}">Signup</a></li>
				{% else %}
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
