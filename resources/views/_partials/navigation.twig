<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<a class="navbar-brand">Furnace</a>
		</div>

		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<li {% if Route.currentRouteNamed('tracks.index') %}class="active"{% endif %}>
					<a href="{{ URL.route('tracks.index') }}">Tracks</a>
				</li>
				<li {% if Route.currentRouteNamed('trackers.index') %}class="active"{% endif %}>
					<a href="{{ URL.route('trackers.index') }}">Blacksmiths</a>
				</li>
				<li>
					<a href="{{ URL.route('ratings.create') }}" class="btn btn-success">Rate a track</a>
				</li>
			</ul>

			<ul class="nav navbar-nav navbar-right">
				{% if Auth.check() %}
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ current_user.name }} <span class="caret"></span></a>
						<ul class="dropdown-menu" role="menu">
							<li><a href="{{ URL.route('auth.logout') }}">Logout</a></li>
						</ul>
					</li>
				{% else %}
					<li><a href="{{ URL.route('auth.login') }}">Login</a></li>
					<li><a href="{{ URL.route('users.create') }}">Signup</a></li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
